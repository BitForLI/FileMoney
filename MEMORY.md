# 项目记忆

最后更新：2026-09-01

## 项目目标

实现一款可安装的轻量 Markdown 桌面应用，提供类似 Obsidian 的基本编辑与知识链接能力。

## 当前状态

- 已实现 Electron + React + TypeScript 桌面应用。
- 已实现 Markdown 编辑/预览、自动保存、多标签、搜索、双向链接、附件和主题设置。
- 左栏采用自由层级：笔记、文件夹和分区都可直接创建在资料库根目录；笔记和文件夹可拖入分区或任意文件夹，也可拖回根目录。
- 应用首次启动自动创建内置资料库，不要求用户选择本地目录。
- 已配置 NSIS Windows 安装器，并通过 GitHub Actions 成功发布 `Pagefold v0.1.5` Windows x64 安装包。
- 已将 Electron 不支持的原生 `prompt` 替换为应用内新建和重命名对话框。
- 已移除创建说明、资料库副标题、空白页教程和右栏教程等提示性文案。
- Markdown 编辑区和渲染预览区都已实现右键菜单：链接到文档、复制、剪切、粘贴、全选；预览选中文字会按出现位置映射回 Markdown 原文，链接选择器提供最近文档和搜索。
- 右键菜单和链接选择框改为在窗口最外层渲染，不再受编辑区模糊滤镜影响而发生坐标偏移或被右侧边界裁切；菜单会自动保持在窗口可见范围内。
- 在编辑区或预览区右键已有 Wiki/Markdown 文档链接时，可直接更改链接目标或取消链接；更改和取消都会保留原显示文字，不再把链接操作置灰。
- “链接到文档”已扩展为两级选择：先选文档，再选整篇文档或具体位置；位置列表默认只显示 Markdown 标题，输入搜索词后才匹配标题、正文、代码和公式。结果按 Markdown/KaTeX 转换后显示，不暴露格式源码。具体位置使用 `#L行号` 锚点，打开链接时编辑区和预览区会滚动到对应行并短暂高亮。
- 右栏可在“链接到本文”和“本文链接”之间切换，分别显示反向链接和当前文档的出站文档链接。
- 已移除编辑区的粗体、斜体等整行格式工具栏；Markdown 编辑和预览能力保持不变。
- 分屏模式中编辑区与预览区之间的竖线可左右拖动，保留两侧最小宽度；支持方向键微调、双击恢复 50/50，并在下次打开时恢复上次比例。
- Markdown 预览已接入 KaTeX，支持 `$...$` 行内公式与 `$$...$$` 独立公式；已确认用户导入的 `CSYS5030_Lecture1_Notes.md` 为正常 UTF-8，先前的“乱码”是公式语法未渲染。
- 已将 KaTeX 统一为 0.16.47，修复渲染结构与 0.18 样式不匹配导致的上下标异常放大；自动预览检查会验证公式下标确实缩小。
- 开发预览配色参考 Obsidian 的白灰布局：浅色模式以 `#F5F5F7`、`#FFFFFF`、`#EDEDF0` 为底，深色模式以 `#27232D`、`#35303D`、`#1D1A22` 为底，使用蓝灰 `#4B6F9F` 做少量强调，区图标使用不同层次的中性灰。
- 应用界面、对话框、设置、编辑器菜单、状态提示和用户可见错误已统一为英文；已有中文笔记、文件夹和正文内容保持原样。
- 左侧仅最外层分区保留编号、卡片和暗红左侧高亮；文件夹使用无编号的普通树状行，笔记不显示编号、红色选中线或文件图标，只显示名称。分区标题为 16px、文件夹为 15px、笔记为 14px。旧分类“我的笔记/你的笔记”仅在界面显示为 `My Notes/Your Notes`，实际路径不变以保护已有链接。
- 文件树改为更紧凑的纵向密度：分区卡片间距 4px、分区标题行 42px、文件夹和笔记行 32px，字号保持不变。
- 分区左侧竖线和编号采用状态色：当前选中的分区或当前笔记所在分区为暗红色，未选中分区为深灰色。
- 当前打开的顶部标签会与左侧文件树同步：对应笔记显示深灰背景和加粗文字，所在分区同步显示暗红状态；通过标签、链接、搜索或反向链接切换均适用。
- 应用显示名称统一为 `Pagefold`，不再显示中文名称或“页”字品牌标识。
- 已实测关闭并重启开发应用，资料库 4 个文件的相对路径、大小和 SHA-256 全部一致，文档树和已打开标签均恢复。
- 支持把外部 `.md` / `.markdown` 文件拖入资料库根目录、分区或文件夹；导入副本并保留原文件，同名时自动追加序号且不覆盖。
- 左栏折叠后的展开图标与折叠前的控制图标保持在同一行；顶部新建笔记和文件夹会默认使用当前选中的分区/文件夹，选中笔记时使用该笔记所在目录。
- 自动保存会在重命名和移动前强制完成，在删除前取消并等待；关闭窗口会先保存全部打开笔记，延迟保存不会再重新创建已删除或已移动的旧路径。
- 重命名和移动会拒绝覆盖同名条目；笔记只允许 `.md`、`.markdown` 和 `.txt`，Windows 保留名称与应用内部目录名称会被拒绝。
- 删除操作按用户确认改为直接永久删除，不再显示确认对话框；删除当前笔记或文件夹后会自动切换到剩余标签。
- 全库搜索重新提供顶部图标和 `Ctrl+P` 入口，设置重新提供可见图标；外部网页链接由系统浏览器打开，不会替换 Pagefold 窗口。
- 多文档预览关闭状态保持稳定，主预览不会被关闭成空白；附加多张图片会一次性插入全部 Markdown，次级预览支持相对图片和文档链接。
- 反向链接同时识别 Wiki 链接与标准 Markdown 文档链接，并在任意打开笔记保存后刷新；完整路径链接可区分不同目录中的同名笔记。
- 文件树的新建与更多操作按钮改为悬浮显示，不再在隐藏状态占用名称宽度；名称只有在侧栏真实空间不足时才显示省略号。
- 点击左侧文件树空白区域会清除当前分区或文件夹选择；随后从顶部新建的笔记或文件夹位于资料库根目录，不属于任何分区。
- 设置中可选择 OneDrive、Dropbox、Syncthing 等同步工具管理的任意本地文件夹作为资料库，也可恢复默认资料库；旧资料库不会自动搬移或删除。应用会监听外部新增、修改和删除并自动刷新，未保存的本地编辑不会被同步变化静默覆盖。
- 修复通过具体行链接进入文档后，每次输入或删除都会重新触发行定位并导致编辑区光标跳动的问题。
- 编辑区会在正文因自动保存或同步刷新时保留当前光标、选区和滚动位置，修复编辑过程中偶尔跳到文末的问题。
- 输入处理已与较重的 Markdown/KaTeX 预览、右栏大纲与链接分析、状态统计分开调度；预览内容复用，减少逐字输入时的卡顿。
- 本地编辑框已改为由浏览器直接接收输入，React 预览、光标辅助状态和自动保存转为后台更新；专项检查在句子中间连续输入 360 个中英文字符，无缺字或跳行，光标位置和磁盘内容一致。该修复尚未发布。
- 本地已将同一文档的自动保存串行化，并在写入前核对磁盘版本；写文件不再先清空，保存失败时窗口不会被 5 秒兜底强制关闭。同步冲突会生成 `-local-conflict` 副本后加载同步版本。
- 本地已修复右栏链接/大纲不跟随当前标签、链接位置异步读取结果串台，并移除未使用的删除确认弹窗代码；这些修复尚未发布。
- 当前最新功能已包含在 GitHub Release `v0.1.5`；本地测试、生产构建和 GitHub Windows 构建均已通过。

## 已确认决定

- 决定：资料库默认由应用管理并位于 `%APPDATA%\pagefold\vault`，用户也可在设置中选择本地同步文件夹。
- 原因：保留免配置的默认体验，同时支持用户通过 Syncthing、OneDrive 或 Dropbox 跨设备同步。
- 日期：2026-08-24
- 决定：分类层级不是强制关系，允许先建根笔记，再通过拖动自由分类。
- 日期：2026-08-24
- 决定：应用品牌只使用英文名 `Pagefold`。
- 日期：2026-08-24
- 决定：主界面使用近白底色、不同层级的黑灰文字和少量暗红强调，不再使用薄荷绿作为主强调色。
- 日期：2026-08-24
- 决定：Pagefold 的应用界面统一使用英文，不自动翻译或重命名用户内容。
- 日期：2026-08-24

## 文件索引

| 文件 | 用途 |
|---|---|
| `src/main/index.ts` | 本地资料库与文件系统 IPC |
| `src/renderer/src/App.tsx` | 应用状态和主要交互 |
| `src/renderer/src/components/FileTree.tsx` | 自由层级文件树和拖动分类 |
| `src/renderer/src/components/EditorPane.tsx` | Markdown 编辑、右键菜单和链接选择器 |
| `package.json` | 开发、测试和 Windows 打包配置 |
| GitHub Release `v0.1.5` | Windows x64 安装包 `Pagefold-Setup-0.1.5.exe` |

## 待解决问题

- [ ] 安装包尚未购买代码签名证书，Windows SmartScreen 可能提示未知发布者。
- [ ] 删除为直接永久删除，尚未实现回收站。
- [ ] 具体位置链接目前依赖行号；若在目标位置之前大量增删内容，原链接可能需要重新选择位置。

## 下一步

1. 用户在 Windows 安装 `v0.1.5`，并在设置中把资料库切换到本地 OneDrive 同步文件夹。
2. 在第二台设备选择对应的本地 OneDrive 文件夹，验证跨设备同步。
3. 后续版本考虑加入回收站和资料库导出/备份。
